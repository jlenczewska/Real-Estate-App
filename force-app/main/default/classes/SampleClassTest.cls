@isTest
public with sharing class SimilarBoatsControllerTest {
    
    @isTest
    static void testMethod1(){

        SampleClass controller = new SampleClass();
        System.assertEquals(controller.method1, 1);
    }

}